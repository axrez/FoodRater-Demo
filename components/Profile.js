import React from 'react';
import { Text, View, Button } from 'react-native';

const Profile = props => {
  return(
    <View>
      <Text>ProfileView</Text>
      <Button title="Go back" onPress={() => null} />
    </View>
  )
}

export default Profile
