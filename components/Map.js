import React from 'react';
import { Text, View } from 'react-native';

const Map = props => (
  <View style={{ flex: 1, alignItems: "center", justifyContent: "center", maxWidth: '75%', marginLeft: '15%' }}>
    <Text>Skal være et kort over omkringliggende resturanter en eller anden dag</Text>
  </View>
);

export default Map;
